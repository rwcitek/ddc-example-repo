from setuptools import setup, find_packages

setup(
    name='python_modules',  # Name of the module
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # Add any dependencies here
)
